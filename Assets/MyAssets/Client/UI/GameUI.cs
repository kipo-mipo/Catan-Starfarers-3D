using UnityEngine;

namespace MyAssets.Client.UI
{
    public sealed class GameUI : MonoBehaviour
    {
        // placeholder for later: phase display, current player, dice, etc.
        public void ShowMessage(string msg) => Debug.Log(msg);
    }
}
